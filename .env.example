# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_DATABASE=thread
DB_TABLE=machine_test

# Serial Port
SERIAL_PORT=/dev/ttyACM0

# Offset correction for stitch length and seam allowance
STITCH_LENGTH_OFFSET_MM=-0.5  # Adjust this value based on calibration (negative
SEAM_ALLOWANCE_MM=4.5          # Add this value to final stitch length